# Required — JWT token provided by workshop organizers
VIRTUAL_CLINIC_TOKEN=your-token-here

# API base URL
VIRTUAL_CLINIC_BASE_URL=https://virtual-clinic-api.vercel.app

# Optional — override which patient to interview (UUID)
# VIRTUAL_CLINIC_PATIENT_ID=

# Optional — task type: diagnosis, treatment, or event
# VIRTUAL_CLINIC_TASK_TYPE=diagnosis
